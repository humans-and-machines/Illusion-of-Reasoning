## Task Utilities (`src/annotate/tasks`)

This directory hosts active, experiment-focused helpers that operate on
annotated JSONL files (e.g., `math_cue_variants.py`).  Legacy wrappers such
as `src.annotate.tasks.shifts` now live in `src.annotate.backcompat.tasks`
so the files here are dedicated to real utilities rather than compatibility
aliases.
